name: cmake-hello
version: 0.1
summary: test the cmake plugin
description: |
  This is a basic cmake snap. It just prints a hello world.

grade: devel
confinement: strict

apps:
  cmake-hello:
    command: cmake-hello

parts:
  cmake-project:
    plugin: cmake
    source: .
    build-packages: [gcc]
